# 编辑器功能测试用例

## 概述

本文档包含编辑器组件的测试用例，涵盖以下4个修复的功能：

1. 快捷输入内容在光标所在位置添加
2. 富文本样式的选中状态反馈
3. 本地图片上传功能（Base64）
4. 链接插入功能（支持设置显示文本）

---

## 测试环境

| 项目 | 说明 |
|------|------|
| 浏览器 | Chrome / Firefox / Safari 最新版 |
| 地址 | http://localhost:3000 |
| 模式 | 富文本模式 / Markdown 模式 |

---

## 测试用例 1: 快捷输入光标位置

### 测试目标
验证快捷输入面板中的内容会在光标所在位置插入，而不是追加到末尾。

### 前置条件
1. 已登录系统
2. 已打开编辑器（富文本模式或Markdown模式）
3. 编辑器中有部分内容

### 测试步骤

#### 测试场景 1.1: 富文本模式 - 中间位置插入
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 在富文本编辑器中输入：`这是第一行` | 内容显示正确 |
| 2 | 按回车换行，输入：`这是第三行` | 内容显示正确 |
| 3 | 将光标移动到第二行（空行）的位置 | 光标在第一行和第三行之间 |
| 4 | 点击工具栏的"😊"按钮（快捷输入） | 快捷输入面板打开 |
| 5 | 点击快捷面板中的"😊 开心" | 面板关闭，文本插入到光标位置 |

**验证**：
- 快捷文本应该插入在第一行和第三行之间
- 最终内容应该是：
  ```
  这是第一行
  😊 开心
  这是第三行
  ```

#### 测试场景 1.2: 富文本模式 - 选中文字替换
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 输入：`今天天气很好` | 内容显示 |
| 2 | 用鼠标选中"很好"两个字 | 文字高亮显示 |
| 3 | 点击快捷输入按钮，选择"😊 开心" | 选中的文字被替换 |

**验证**：
- 最终内容应该是：`今天天气😊 开心`
- 而不是：`今天天气很好😊 开心`

#### 测试场景 1.3: Markdown模式 - 光标位置插入
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 切换到 Markdown 模式 | 编辑器显示 textarea |
| 2 | 输入：`# 标题` | 内容显示 |
| 3 | 换行，输入：`## 二级标题` | 内容显示 |
| 4 | 将光标移动到两个标题之间 | 光标在第一行末尾或第二行开头 |
| 5 | 点击快捷输入，选择"今天天气很好" | 文本插入到光标位置 |

**验证**：
- 最终内容应该是：
  ```
  # 标题
  今天天气很好
  ## 二级标题
  ```

#### 测试场景 1.4: 连续快捷输入
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 确保编辑器为空 | 无内容 |
| 2 | 点击快捷输入，选择"😊 开心" | 文本插入到开头 |
| 3 | 再次点击快捷输入，选择"今天天气很好" | 文本插入到光标位置（之前插入文本的后面） |
| 4 | 再次点击快捷输入，选择"工作顺利完成" | 文本继续追加 |

**验证**：
- 三个快捷输入的文本应该按顺序连接
- 光标应该始终在最后插入的文本后面

---

## 测试用例 2: 富文本工具栏选中状态

### 测试目标
验证富文本编辑器的工具栏按钮（粗体、斜体、下划线等）在选中文本时会有选中状态反馈。

### 前置条件
1. 已登录系统
2. 已打开编辑器
3. 处于富文本模式

### 测试步骤

#### 测试场景 2.1: 粗体按钮选中状态
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 输入：`这是测试文字` | 内容显示 |
| 2 | 用鼠标选中"测试"两个字 | 文字高亮 |
| 3 | 点击工具栏的粗体按钮（B） | "测试"文字变粗 |
| 4 | **保持选中状态**，观察按钮样式 | 粗体按钮应该有蓝色背景（active状态） |
| 5 | 将光标移动到加粗的文字中间 | 光标在"测"和"试"之间 |
| 6 | 观察粗体按钮样式 | 按钮仍然保持选中状态 |
| 7 | 再次点击粗体按钮 | 文字恢复正常，按钮状态取消 |

**验证点**：
- 选中文本点击粗体后，按钮有 `active` 样式（蓝色背景）
- 光标在加粗文字中时，按钮保持选中状态
- 光标在普通文字中时，按钮取消选中状态

#### 测试场景 2.2: 斜体按钮选中状态
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 输入：`斜体测试` | 内容显示 |
| 2 | 选中全部文字 | 高亮显示 |
| 3 | 点击斜体按钮（I） | 文字变斜体 |
| 4 | 观察按钮样式 | 斜体按钮有蓝色背景 |
| 5 | 在斜体文字后面继续输入 | 新输入的文字也应该是斜体 |
| 6 | 观察按钮状态 | 按钮保持选中 |

#### 测试场景 2.3: 标题按钮选中状态
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 输入：`这是标题` | 内容显示 |
| 2 | 点击 H1 按钮 | 文字变成一级标题样式 |
| 3 | 观察 H1 按钮 | 应该有选中状态 |
| 4 | 点击 H2 按钮 | 文字变成二级标题 |
| 5 | 观察按钮 | H1 取消选中，H2 选中 |
| 6 | 点击正文（非标题按钮） | 文字恢复正常大小 |
| 7 | 观察按钮 | 所有标题按钮都取消选中 |

**注意**：标题按钮的选中状态可能需要特别验证，因为 `queryCommandValue('formatBlock')` 返回的是标签名。

#### 测试场景 2.4: 列表按钮选中状态
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 输入几行文字，每行换行 | 多行文字 |
| 2 | 选中前两行 | 高亮显示 |
| 3 | 点击无序列表按钮（•） | 文字变成列表项 |
| 4 | 观察按钮 | 无序列表按钮选中 |
| 5 | 光标在列表项中 | 按钮保持选中 |
| 6 | 点击有序列表按钮（1.） | 列表变成有序列表 |
| 7 | 观察按钮 | 无序列表取消，有序列表选中 |

#### 测试场景 2.5: 键盘快捷键同步
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 输入：`快捷键测试` | 内容显示 |
| 2 | 选中文字 | 高亮 |
| 3 | 按 `Ctrl+B` (Mac: `Cmd+B`) | 文字变粗体 |
| 4 | 观察粗体按钮 | 按钮应该有选中状态 |
| 5 | 按 `Ctrl+I` | 文字同时变斜体 |
| 6 | 观察按钮 | 粗体和斜体按钮都选中 |

---

## 测试用例 3: 本地图片上传

### 测试目标
验证点击图片按钮时会打开文件选择器，选择本地图片后以 Base64 格式插入到编辑器中。

### 前置条件
1. 已登录系统
2. 已打开编辑器
3. 准备测试图片（PNG、JPG、GIF 等）

### 测试步骤

#### 测试场景 3.1: 富文本模式 - 图片上传
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 确保在富文本模式 | 编辑器是 contenteditable div |
| 2 | 输入一些文字，然后将光标移到想要插入图片的位置 | 光标位置确定 |
| 3 | 点击工具栏的图片按钮（🖼） | **打开文件选择对话框**（而不是 prompt 输入URL） |
| 4 | 选择一张本地图片（PNG 或 JPG） | 文件选择器关闭 |
| 5 | 观察编辑器 | 图片应该插入到光标位置 |
| 6 | 检查图片源 | 检查图片的 src 应该是 base64 格式（data:image/...） |

**验证方法（检查Base64）**：
1. 右键点击插入的图片，选择"检查"
2. 查看 `<img>` 标签的 `src` 属性
3. 应该以 `data:image/` 开头，例如：
   ```html
   <img src="data:image/png;base64,iVBORw0KGgo...">
   ```

#### 测试场景 3.2: Markdown模式 - 图片上传
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 切换到 Markdown 模式 | 显示 textarea 和预览区 |
| 2 | 点击图片按钮 | 打开文件选择器 |
| 3 | 选择一张图片 | 文件选择器关闭 |
| 4 | 观察 textarea | 应该插入 Markdown 图片语法 |

**验证**：
- 应该插入类似这样的内容：
  ```markdown
  ![test.png](data:image/png;base64,iVBORw0KGgo...)
  ```
- 预览区应该能正常显示图片

#### 测试场景 3.3: 选中文字后插入图片
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 输入：`这里有一张图片` | 内容显示 |
| 2 | 选中"一张图片"四个字 | 文字高亮 |
| 3 | 点击图片按钮，选择图片 | 文件选择器打开 |
| 4 | 选择图片后 | 选中的文字被图片替换 |

**验证**：
- 最终内容应该是：`这里有[图片]`，而不是`这里有一张图片[图片]`

#### 测试场景 3.4: 非图片文件选择
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 点击图片按钮 | 文件选择器打开 |
| 2 | 尝试选择一个非图片文件（如 .txt, .pdf） | 根据浏览器设置可能无法选择或有提示 |

**预期行为**：
- 文件选择器的 `accept="image/*"` 应该只显示图片文件
- 如果用户强行选择非图片，应该有错误提示：`请选择图片文件`

#### 测试场景 3.5: 图片样式
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 插入一张较大的图片（宽度超过编辑器） | 图片插入 |
| 2 | 观察图片显示 | 图片应该有 `max-width: 100%` 样式 |
| 3 | 不会出现横向滚动条 | 图片自适应编辑器宽度 |

---

## 测试用例 4: 链接插入功能

### 测试目标
验证插入链接时可以设置显示文本，点击文本可以跳转到链接地址。

### 前置条件
1. 已登录系统
2. 已打开编辑器
3. 处于富文本模式

### 测试步骤

#### 测试场景 4.1: 无选中文本 - 插入链接
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 确保编辑器为空，光标在开头 | 无选中内容 |
| 2 | 点击链接按钮（🔗） | **第一个 prompt 出现：请输入链接显示文本** |
| 3 | 输入：`百度` | 点击确定 |
| 4 | **第二个 prompt 出现**：请输入链接地址 | 显示默认值 `https://` |
| 5 | 输入：`https://www.baidu.com` | 点击确定 |
| 6 | 观察编辑器 | 应该显示"百度"文字，且是蓝色可点击链接 |

**验证**：
- 显示的是"百度"而不是URL
- 悬停时应该有下划线
- 检查HTML：应该是 `<a href="https://www.baidu.com" target="_blank">百度</a>`
- 点击链接应该在新窗口打开百度

#### 测试场景 4.2: 有选中文本 - 插入链接
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 输入：`访问谷歌搜索更多内容` | 内容显示 |
| 2 | 选中"谷歌"两个字 | 文字高亮 |
| 3 | 点击链接按钮 | **只出现一个 prompt：请输入链接地址** |
| 4 | 输入：`https://www.google.com` | 点击确定 |
| 5 | 观察编辑器 | "谷歌"变成蓝色链接 |

**验证**：
- 因为已经有选中文本"谷歌"，所以不需要再输入显示文本
- 最终效果：`访问[谷歌]搜索更多内容`
- 链接应该可以点击跳转

#### 测试场景 4.3: 取消链接输入
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 光标在编辑器中，无选中内容 | 准备插入链接 |
| 2 | 点击链接按钮 | 显示文本 prompt 出现 |
| 3 | 点击"取消"按钮 | prompt 关闭，无操作 |
| 4 | 验证 | 编辑器内容没有变化 |

#### 测试场景 4.4: 链接属性验证
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 插入一个链接（如"测试" -> https://example.com） | 链接显示 |
| 2 | 右键检查链接元素 | 打开开发者工具 |
| 3 | 验证属性 | 检查 HTML 结构 |

**应该满足**：
```html
<a href="https://example.com" target="_blank">测试</a>
```

关键点：
- `href` 是正确的URL
- `target="_blank"` 确保在新窗口打开
- 没有 `target="_self"` 会导致在当前窗口跳转，可能丢失未保存的编辑内容

#### 测试场景 4.5: Markdown模式链接插入
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 切换到 Markdown 模式 | 显示 textarea |
| 2 | 无选中内容，点击链接按钮 | 先输入显示文本 prompt |
| 3 | 输入显示文本：`GitHub` | 确定 |
| 4 | 输入URL：`https://github.com` | 确定 |
| 5 | 观察 textarea | 应该插入 Markdown 链接语法 |

**验证**：
- 应该插入：`[GitHub](https://github.com)`
- 预览区应该显示蓝色可点击的链接

#### 测试场景 4.6: 链接点击跳转（Ctrl/Cmd + 点击）
**新增测试场景 - 核心功能**

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 在富文本模式下插入一个链接（如"百度" -> https://www.baidu.com） | 链接显示为蓝色 |
| 2 | 按住 **Ctrl键**（Windows/Linux）或 **Cmd键**（Mac） | 保持按键按下 |
| 3 | **点击**链接文字 | 链接应该在新窗口打开 |
| 4 | 验证 | 新标签页打开了百度网站 |

**关键点**：
- 按住 Ctrl/Cmd 键 + 点击 = 直接在新窗口打开
- **不会**弹出确认对话框
- 这是最常用的快捷操作方式

#### 测试场景 4.7: 链接点击跳转（普通点击 - 确认对话框）
**新增测试场景**

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 插入一个链接（如"谷歌" -> https://www.google.com） | 链接显示 |
| 2 | **不按任何键**，直接点击链接 | 应该弹出确认对话框 |
| 3 | 观察对话框内容 | 应该显示链接地址和提示信息 |
| 4 | 点击"确定" | 链接在新窗口打开 |
| 5 | 或点击"取消" | 对话框关闭，不打开链接 |

**对话框应该显示**：
```
是否在新窗口打开链接？

https://www.google.com

提示：按住 Ctrl/Cmd 键点击链接可直接打开

[确定]  [取消]
```

#### 测试场景 4.8: 链接悬停提示
**新增测试场景**

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 插入一个链接 | 链接显示 |
| 2 | 将鼠标悬停在链接上（不点击） | 鼠标指针变成 **手形** (pointer) |
| 3 | 观察浏览器状态栏或 tooltip | 应该显示链接地址 |
| 4 | 检查 tooltip 内容 | 应该包含提示："按住 Ctrl/Cmd 键点击可直接在新窗口打开" |

**验证**：
- 悬停时 `cursor: pointer`
- tooltip 显示链接地址和使用提示

#### 测试场景 4.9: 无效链接处理
**新增测试场景**

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 手动创建一个无效链接（如 href="#" 或 href="javascript:void(0)"） | 通过开发者工具或其他方式 |
| 2 | 点击该链接 | 应该**不会**触发跳转行为 |
| 3 | 验证 | 不会打开新窗口，不会弹出对话框 |

**被忽略的链接类型**：
- `href="#"` - 锚点链接
- `href="javascript:..."` - JavaScript 链接
- `href=""` 或 `href` 为空 - 无地址链接

---

## 测试执行清单

### 功能测试清单

| # | 功能模块 | 测试场景 | 测试状态 | 备注 |
|---|----------|----------|----------|------|
| 1 | 快捷输入光标位置 | 1.1 富文本中间插入 | ⬜ 待测试 | |
| 2 | 快捷输入光标位置 | 1.2 选中文字替换 | ⬜ 待测试 | |
| 3 | 快捷输入光标位置 | 1.3 Markdown光标位置 | ⬜ 待测试 | |
| 4 | 快捷输入光标位置 | 1.4 连续快捷输入 | ⬜ 待测试 | |
| 5 | 工具栏选中状态 | 2.1 粗体按钮 | ⬜ 待测试 | **核心功能** |
| 6 | 工具栏选中状态 | 2.2 斜体按钮 | ⬜ 待测试 | |
| 7 | 工具栏选中状态 | 2.3 标题按钮 | ⬜ 待测试 | |
| 8 | 工具栏选中状态 | 2.4 列表按钮 | ⬜ 待测试 | |
| 9 | 工具栏选中状态 | 2.5 键盘快捷键 | ⬜ 待测试 | |
| 10 | 本地图片上传 | 3.1 富文本上传 | ⬜ 待测试 | **核心功能** |
| 11 | 本地图片上传 | 3.2 Markdown上传 | ⬜ 待测试 | |
| 12 | 本地图片上传 | 3.3 选中替换 | ⬜ 待测试 | |
| 13 | 本地图片上传 | 3.4 非图片文件 | ⬜ 待测试 | |
| 14 | 本地图片上传 | 3.5 图片样式 | ⬜ 待测试 | |
| 15 | 链接插入 | 4.1 无选中插入 | ⬜ 待测试 | **核心功能** |
| 16 | 链接插入 | 4.2 有选中插入 | ⬜ 待测试 | **核心功能** |
| 17 | 链接插入 | 4.3 取消输入 | ⬜ 待测试 | |
| 18 | 链接插入 | 4.4 链接属性 | ⬜ 待测试 | |
| 19 | 链接插入 | 4.5 Markdown链接 | ⬜ 待测试 | |
| 20 | 链接点击跳转 | 4.6 Ctrl/Cmd+点击 | ⬜ 待测试 | **核心功能 - 新增** |
| 21 | 链接点击跳转 | 4.7 普通点击+确认 | ⬜ 待测试 | **新增** |
| 22 | 链接点击跳转 | 4.8 悬停提示 | ⬜ 待测试 | **新增** |
| 23 | 链接点击跳转 | 4.9 无效链接处理 | ⬜ 待测试 | **新增** |

### 浏览器兼容性测试

| 浏览器 | 版本 | 快捷输入 | 工具栏状态 | 图片上传 | 链接插入 | 链接跳转 |
|--------|------|----------|------------|----------|----------|----------|
| Chrome | 最新 | ⬜ | ⬜ | ⬜ | ⬜ | ⬜ |
| Firefox | 最新 | ⬜ | ⬜ | ⬜ | ⬜ | ⬜ |
| Safari | 最新 | ⬜ | ⬜ | ⬜ | ⬜ | ⬜ |
| Edge | 最新 | ⬜ | ⬜ | ⬜ | ⬜ | ⬜ |

---

## 附录：代码修改摘要

### 修改的文件

| 文件 | 修改内容 |
|------|----------|
| `index.html` | 添加隐藏的 `<input type="file" id="image-upload-input">` |
| `js/components/editor.component.js` | 重写大部分功能，**新增链接点击跳转处理** |
| `css/style.css` | 添加 `.toolbar-btn.active` 样式 |

### 主要修复点

**1. 快捷输入光标位置问题**
- 新增变量：`savedSelection`（富文本选区）、`savedTextareaSelection`（Markdown选区）
- 在编辑器 `blur` 时保存选区
- 点击工具栏按钮时 `preventDefault()` 防止失焦
- 插入文本前 `restoreSelection()` 恢复选区

**2. 工具栏选中状态**
- 监听 `document.selectionchange` 事件
- 使用 `document.queryCommandState('bold')` 等检查状态
- 使用 `document.queryCommandValue('formatBlock')` 检查标题格式
- 根据状态动态添加/移除 `active` 类

**3. 本地图片上传**
- 添加隐藏的文件输入框
- 点击图片按钮时 `saveSelection()` 然后触发文件选择
- 使用 `FileReader.readAsDataURL()` 读取为 Base64
- 插入 `<img>` 元素（富文本）或 Markdown 图片语法

**4. 链接插入改进**
- 检查 `window.getSelection().toString()` 是否有内容
- 有选中文本：只需要输入 URL
- 无选中文本：先输入显示文本，再输入 URL
- 使用 `insertHTML` 插入带 `target="_blank"` 的链接

**5. 链接点击跳转（新增修复）**
- 新增 `handleLinkClick()` 函数处理链接点击事件
- **Ctrl/Cmd + 点击**：直接在新窗口打开，无确认对话框
- **普通点击**：弹出确认对话框，显示链接地址和使用提示
- 新增 `handleLinkMouseOver()`：悬停时显示 tooltip，设置 `cursor: pointer`
- 新增 `handleLinkMouseOut()`：鼠标离开时恢复
- **无效链接过滤**：`href="#"`、`href="javascript:..."` 等不会触发跳转

### 链接点击实现逻辑

```javascript
function handleLinkClick(e) {
    const link = e.target.closest('a');
    if (!link) return;
    
    const href = link.getAttribute('href');
    
    // 按住 Ctrl/Cmd 键 + 点击 = 直接打开
    if (e.ctrlKey || e.metaKey) {
        e.preventDefault();
        window.open(href, '_blank');
        return;
    }
    
    // 普通点击 = 弹出确认对话框
    e.preventDefault();
    const openLink = confirm(`是否在新窗口打开链接？\n\n${href}\n\n提示：按住 Ctrl/Cmd 键点击链接可直接打开`);
    if (openLink) {
        window.open(href, '_blank');
    }
}
```

### 设计考量

1. **安全性**：在 `contenteditable` 编辑器中，默认链接点击可能导致导航离开编辑页面，丢失未保存内容
2. **用户体验**：
   - 高级用户可以使用 `Ctrl/Cmd + 点击` 快速打开
   - 普通用户有确认对话框，防止误触
3. **提示信息**：对话框和 tooltip 都提示快捷键方式，帮助用户学习
4. **无效链接过滤**：避免处理 JavaScript 链接和空链接
